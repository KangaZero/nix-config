# ≈ lua/plugins/ai.lua
#
# The real config has copilot entirely commented out, so it is disabled here too
# rather than silently enabling something the primary editor does not load.
# Flip `enable` to true and the commented settings in ai.lua map onto setupOpts.
{
  config.vim.assistant.copilot.enable = false;
}
